═══════════════════════════════════════════════════════════════════
PROMPT TO GIVE CLAUDE CODE ON VULTR VM (seed1.ammocoin.build)
═══════════════════════════════════════════════════════════════════

Copy this entire message and send it to Claude Code running on your Vultr VM:

───────────────────────────────────────────────────────────────────

I need you to build AMMOcoin v1.1.0 Linux x86_64 binaries with the correct genesis block.

First, stop any running daemon and clean up old builds:
- Stop ammocoin daemon if running
- Remove any old AMMOcoin-build or ammocoin-v1.1.0-temp directories

Then clone fresh from GitHub:
```
git clone https://github.com/everquin/AMMOcoin-v1.1.0.git AMMOcoin-build
```

IMPORTANT: Verify the genesis hash before building:
```
grep "00000da28f340848df488cd5cd49e4f40dbce9d37ab8fb9e8ec400b40b8fb162" ~/AMMOcoin-build/ammocoin-apps-from-ammocoin/src/chainparams.cpp
```

If that hash is NOT found, STOP - we have the wrong code.

If found, proceed to build:
```
cd ~/AMMOcoin-build/ammocoin-apps-from-ammocoin
./autogen.sh
./configure --disable-tests --disable-bench --without-gui --enable-wallet --with-incompatible-bdb
make -j$(nproc)
```

After build completes, verify:
1. Check binaries exist: src/ammocoind, src/ammocoin-cli, src/ammocoin-tx
2. Verify genesis in binary: `strings src/ammocoind | grep 00000da28f340848`
3. Check version: `src/ammocoind --version` (should be v1.1.0)

Create release package:
```
mkdir -p ~/ammocoin-linux-release/bin
cp src/ammocoind src/ammocoin-cli src/ammocoin-tx ~/ammocoin-linux-release/bin/
cd ~/ammocoin-linux-release
tar -czf ~/AMMOcoin-v1.1.0-Linux-x86_64.tar.gz .
cd ~
sha256sum AMMOcoin-v1.1.0-Linux-x86_64.tar.gz > AMMOcoin-v1.1.0-Linux-x86_64-CHECKSUM.txt
```

Show me:
- Final binary sizes
- The checksum file contents
- Confirmation genesis hash is in the binary

───────────────────────────────────────────────────────────────────

ALTERNATIVE: If Claude Code can execute bash scripts, you can send this instead:

───────────────────────────────────────────────────────────────────

Download and run the automated build script:

```bash
curl -o vultr-build.sh https://raw.githubusercontent.com/everquin/AMMOcoin-v1.1.0/main/vultr-quick-build.sh
chmod +x vultr-build.sh
./vultr-build.sh
```

Or if you have the script locally:
```bash
bash ~/vultr-quick-build.sh
```

───────────────────────────────────────────────────────────────────

AFTER BUILD COMPLETES ON VULTR:

On your Mac, download the binaries:

```bash
# Download archive and checksum
scp root@seed1.ammocoin.build:~/AMMOcoin-v1.1.0-Linux-x86_64.tar.gz /Volumes/CRUCIAL_2TB/GITHUB/AMMOcoin/releases/
scp root@seed1.ammocoin.build:~/AMMOcoin-v1.1.0-Linux-x86_64-CHECKSUM.txt /Volumes/CRUCIAL_2TB/GITHUB/AMMOcoin/releases/

# Verify download
ls -lh /Volumes/CRUCIAL_2TB/GITHUB/AMMOcoin/releases/AMMOcoin-v1.1.0-Linux-x86_64*
cat /Volumes/CRUCIAL_2TB/GITHUB/AMMOcoin/releases/AMMOcoin-v1.1.0-Linux-x86_64-CHECKSUM.txt
```

Then tell Claude Code (on Mac):

"I've downloaded the Linux x86_64 binaries from Vultr. They're at:
/Volumes/CRUCIAL_2TB/GITHUB/AMMOcoin/releases/AMMOcoin-v1.1.0-Linux-x86_64.tar.gz

Ready to package both macOS and Linux releases and create checksums."

═══════════════════════════════════════════════════════════════════
